# Root folder where your repos live (each repo is a direct child, e.g. c:\code\repos\YourOrg.YourRepo)
REPOS_ROOT=c:\code\repos

# Cursor Agent API key (optional if you use 'agent login' instead).
# Get a key from https://cursor.com/settings
# CURSOR_API_KEY=your_key_here

# Full path to the agent CLI (optional; use if 'agent' is not on your PATH).
# AGENT_PATH=/path/to/agent

# Azure DevOps (optional; when set, you can pass a PR ID or URL).
# Use either set of names (CodeHermit or mcp-server style).
# AZURE_ORG_URL=https://dev.azure.com/YourOrg
# AZURE_PROJECT=YourProject
# AZURE_PAT=your_personal_access_token
# Or: AZURE_DEVOPS_ORG=YourOrg, AZURE_DEVOPS_PROJECT=YourProject, AZURE_DEVOPS_PAT=...
# PAT must have at least Code (Read) scope. Create at: https://dev.azure.com/_usersSettings/tokens
